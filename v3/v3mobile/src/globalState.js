// src/globalState.js
import { reactive } from "vue";

// Define your global state object
const globalState = reactive({
  globalVariable: false, // Initialize your global variable
});

export default globalState;
